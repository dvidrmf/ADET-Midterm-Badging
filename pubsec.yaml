name: Badging
description: ADET Midterm Badging

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
```

**Step 2 — in the bottom panel, click `Pub Commands` → then click `pub get`**

Like this in FlutLab's bottom bar:
```
Outputs | Analyzer 23 | Pub Commands ← click here | Tests | Tools
